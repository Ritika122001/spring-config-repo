server:
  port: 8090

spring:
  application:
    name: order-service
    
logging:
  level:
    root: INFO

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
