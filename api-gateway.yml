spring:
    application:
        name : api-gateway
    
server:
    port: 8571

logging:
  level:
    root: INFO
    
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
